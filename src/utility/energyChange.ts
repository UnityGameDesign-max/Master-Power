export const energyChange = (lineSvgOne : boolean, strokeColor :string) => {
    switch(lineSvgOne){
        case true:
            strokeColor = '#EA1313';
            return strokeColor;
        break;
    }
} 